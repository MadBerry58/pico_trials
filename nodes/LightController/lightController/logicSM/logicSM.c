/* 
    Initial state:          LED_off
    No pattern received:    (LED_off)
    Static RGB received:    (Static_pattern)  <update_pattern>
    Dynamic RGB received:   (Dynamic_pattern) <update_pattern>
    White RGB received:     (White_pattern)   <update_pattern>

    Initial state:          Static_pattern:
    No pattern received:    (Static_pattern)
    Static RGB received:    (Static_pattern)  <update_pattern>
    Dynamic RGB received:   (Dynamic_pattern) <update_pattern>
    White RGB received:     (White_pattern)   <update_pattern>

    Initial state:          Dynamic_pattern:
    No pattern received:    (Dynamic_pattern) <update_pattern>
    Static RGB received:    (Static_pattern)  <update_pattern>
    Dynamic RGB received:   (Dynamic_pattern) <update_pattern>
    White RGB received:     (White_pattern)   <update_pattern>

    Initial state:          White_pattern:
    No pattern received:    (White_pattern)
    Static RGB received:    (Static_pattern)  <update_pattern>
    Dynamic RGB received:   (Dynamic_pattern) <update_pattern>
    White RGB received:     (White_pattern)   <update_pattern>
 */