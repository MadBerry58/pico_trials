#ifndef GPIO_H
#define GPIO_H

#include <stdio.h>

/* 
typedef struct{
    boolean     coreID;
    uint32_t    reservedGPIOmask;
    uint8_t[32] gpioFunctionality; //function of each gpio pin
} gpioConfig;
*/

///TODO: add gpio init overlay for reserve checking
///TODO: add overlay for concurrent dynamic access

#endif